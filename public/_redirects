/auth /auth/index.html 200
/auth/ /auth/index.html 200
/auth.html /auth/index.html 200
# Apple looks for the Sign in with Apple domain proof under /.well-known/.
# Dot-prefixed files are unreliable through the Pages upload, so the file lives
# at the root and is served from the path Apple checks.
/.well-known/apple-developer-domain-association.txt /apple-developer-domain-association.txt 200
/* /index.html 200
